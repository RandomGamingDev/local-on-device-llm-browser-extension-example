{
  "name": "gemma-llm-local-extension-boilerplate",
  "version": "1.0.0",
  "description": "Run Mediapipe LLMs locally in browser",
  "scripts": {
    "copy-models": "node scripts/copy-models.js",
    "build": "webpack --mode production && npm run copy-models",
    "dev": "webpack --mode development --watch && npm run copy-models"
  },
  "dependencies": {
    "@mediapipe/tasks-genai": "^0.10.15"
  },
  "devDependencies": {
    "copy-webpack-plugin": "^11.0.0",
    "webpack": "^5.103.0",
    "webpack-cli": "^5.1.4"
  }
}
